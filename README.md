## Two Ways Bindings in ReactJS

### This App contains a parent and a child component. Child component has an image object, whose size get changed by parent component also by some other child component. 

